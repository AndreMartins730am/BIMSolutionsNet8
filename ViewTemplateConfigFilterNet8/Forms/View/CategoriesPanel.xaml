<UserControl x:Class="ViewTemplateConfigFilterNet8.Forms.View.CategoriesPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ViewTemplateConfigFilterNet8.Forms.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="MyWaterMarkStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Grid>
                            <Border Background="White" BorderBrush="#FF7D8683" BorderThickness="1"/>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,0,0,0" VerticalAlignment="Center" />
                            <Label Margin="5,0,0,0" 
                                   x:Name="WaterMarkLabel" 
                                   Content="{TemplateBinding Tag}" 
                                   VerticalAlignment="Center"
                                   Visibility="Collapsed" 
                                   Foreground="Gray" 
                                   FontFamily="Arial"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Visibility" TargetName="WaterMarkLabel" Value="Visible"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="DimGray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>


        <Label Grid.Row="0" Content="CATEGORIAS:" FontWeight="Bold"/>
        <!-- Header with Select All -->
        <DockPanel Grid.Row="1">           
            <TextBlock Text="{Binding Header, ElementName=Root}" 
                       FontWeight="SemiBold" VerticalAlignment="Center"/>
            <CheckBox Content="SELECIONAR TODOS"
                      Margin="5,0,0,0"
                      IsChecked="{Binding SelectAll, Mode=TwoWay}"
                      VerticalAlignment="Center"
                      DockPanel.Dock="Right"/>
        </DockPanel>
        <!-- Filter -->
        <TextBox Grid.Row="2" 
                   Margin="5,6,5,6"
                   Style="{StaticResource MyWaterMarkStyle}"
                   Text="{Binding FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                   Tag="Filtrar..."/>

        <!-- ListBox of CheckBoxes -->
        <!--ItemsSource="{Binding CategoriesCollection}"-->
        <ListBox Grid.Row="3"
                 Margin="5,0,5,6"
                 ItemsSource="{Binding ViewItems}"
                 SelectionMode="Multiple">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Name}"
                              IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              Padding="4,2"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ListBox Grid.Row="4"
                 Margin="5,0,5,6"
                 ItemsSource="{Binding SelectedCategoriesView}"
                 >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" Padding="4,2"/>
                </DataTemplate>                
            </ListBox.ItemTemplate>             
        </ListBox>
        <Label Grid.Row="5"
               HorizontalAlignment="Right"
               VerticalAlignment="Center"
               FontSize="14"
               Margin="0,0,5,5"
               Content="{Binding SelectedCountText}"/>
    </Grid>
</UserControl>
